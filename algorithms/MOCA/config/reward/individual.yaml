# @package _global_
# Individual-reward variant: each agent keeps its own apples (the social dilemma
# contracts are meant to mitigate).
ENV_KWARGS:
  shared_rewards: False
REWARD: individual
WANDB_TAGS:
  - INDIVIDUAL_REWARD
